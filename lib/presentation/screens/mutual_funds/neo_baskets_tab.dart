import 'package:flutter/material.dart';

import 'neo_baskets_listview.dart';

class NeoBasketsTabView extends StatelessWidget {
  const NeoBasketsTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const NeoBasketsListView();
  }
}
